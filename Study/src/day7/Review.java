package day7;

public class Review {
	
	/*	변수 -> 자료형 -> 형변환
	-> 연산자 
		산술 - %
		비교 - <=, >=, ==, !=
		논리 - &&, ||, !
		증감 - ++a, a++
		비트
		조건 - A ? B : C
		대입
	-> 조건문
		if(조건식)
		switch(특정값) case
	-> 반복문
		for
		향상된 for문 
			int arr = new int[4];
				for(int a : arr) { // = for(int i=0;i<4;i++){ int a = arr[i] }
					
				}
		while
		do-while
	-> 배열
		할당을 new 를 사용, 번지는 [3] 표시, xxx.length (0 ~ 1사이의 번지)
	-> 클래스
		멤버변수;
		멤버메소드;
		생성자
		접근제한자(private < default < protected <public), 리턴, 메소드(매개변수){*/

}
